import { FC } from 'react'
import { useGlobalContext } from '../Context/store'

interface pageProps {
   
  }
  
  const Page: FC<pageProps> = async () => {


  return <div>
    harsh
  </div>
}

export default Page